services:
  - type: web
    name: ponto-lanchonete
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /manifest.json
    envVars:
      - key: DATABASE_URL
        sync: false   # connection string do Neon
      - key: ADMIN_PASSWORD
        sync: false   # senha da área administrativa
